#include <bits/stdc++.h>

using namespace std;

struct Trie {
	vector<Trie*> link;

	Trie() {}
	
	void insert(string &word, int len) {
		if(len == key.length()) {
			return new Trie(
		}
	}	
};

int main() { return 0; }